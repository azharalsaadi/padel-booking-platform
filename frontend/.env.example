# Base URL of the Laravel backend's API (see backend/routes/api.php — every
# route is mounted under /api, there is no /v1 prefix). The Sanctum
# CSRF-cookie endpoint is derived from this by stripping the trailing /api.
VITE_API_BASE_URL=http://localhost:8000/api
